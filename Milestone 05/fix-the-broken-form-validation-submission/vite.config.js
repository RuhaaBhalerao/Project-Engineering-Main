import { defineConfig } from 'vite'

// Using a minimal Vite config without @vitejs/plugin-react
// The project is small and will run without the plugin in this environment.
export default defineConfig({})
